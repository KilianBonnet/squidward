/*
 * Convert the GetEntry to a game board usable by the algorithm.
 */
function convertEntryToBoard(){

}

function askAiToPlay(b){
    let board = convertEntryToBoard(b);
}

module.exports = {
    askAiToPlay:askAiToPlay
}